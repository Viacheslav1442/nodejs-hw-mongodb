summary: Створити новий контакт
tags:
  - Contacts
requestBody:
  required: true
  content:
    multipart/form-data:
      schema:
        $ref: '../../components/schemas.yaml#/components/schemas/ContactCreate'
responses:
  '201':
    description: Контакт створено
    content:
      application/json:
        schema:
          $ref: '../../components/schemas.yaml#/components/schemas/Contact'
  '400':
    description: Невірні дані
  '401':
    description: Неавторизований доступ
